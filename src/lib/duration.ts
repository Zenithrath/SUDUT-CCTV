export function hoursMinutesToMinutes(hours: number, minutes: number) { if (!Number.isInteger(hours) || !Number.isInteger(minutes) || hours < 0 || hours > 24 || minutes < 0 || minutes > 59 || (hours === 24 && minutes !== 0)) throw new Error("Durasi tidak valid"); return hours * 60 + minutes; }
export function minutesToHoursMinutes(totalMinutes: number) { if (!Number.isInteger(totalMinutes) || totalMinutes < 0) throw new Error("Menit tidak valid"); return { hours: Math.floor(totalMinutes / 60), minutes: totalMinutes % 60 }; }
export function formatDuration(totalMinutes: number) { const { hours, minutes } = minutesToHoursMinutes(totalMinutes); return `${String(hours).padStart(2,"0")}:${String(minutes).padStart(2,"0")}`; }
export function parsePastedDuration(value: string) { const raw = value.trim(); if (!raw) return null; const clock = raw.match(/^(\d{1,2}):(\d{2})$/); if (clock) return hoursMinutesToMinutes(Number(clock[1]), Number(clock[2])); if (/^\d+(\.\d+)?$/.test(raw)) { const number = Number(raw); const hours = Math.floor(number); return hoursMinutesToMinutes(hours, Math.round((number - hours) * 60)); } throw new Error("Format waktu tidak dikenali"); }
export function calculateUptimePercentage(uptimeMinutes: number, availableMinutes: number) { return availableMinutes ? (uptimeMinutes / availableMinutes) * 100 : 0; }
export function getDaysInSelectedMonth(year: number, month: number) { return new Date(year, month, 0).getDate(); }
